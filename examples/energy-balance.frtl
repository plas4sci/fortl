""" 
0D Energy Balance Model
"""
π              : Float[Unit[1]] = 3.14159265359
solar_constant : Float[Unit[J / (s * m^2)]]       = 1370.0
mean_radius    : Float[Unit[m]] = 371.0e3

"""Stefan-Boltzmann's constant"""
sigma          : Float[Unit[J / (s * m^2 * K^4)]] = 5.67e-8

def energy_in(albedo : Float[Unit[1]]) -> Float[Unit[J / s]]:
  return (solar_constant * π * (mean_radius^2) * ((1.0 : Float[Unit[1]]) - albedo))

def energy_out(temperature : Float[Unit[K]]) -> Float[Unit[J / s]]:
  return (sigma * temperature^4 * (4.0 : Float[Unit[1]]) * π * mean_radius^2)

earth_energy_in  = energy_in(0.3)
earth_energy_out = energy_out(288.0)
it = (earth_energy_in, earth_energy_out)